[project]
name = "web-search-examples"
version = "0.1.0"
description = "Web search cookbook examples for GL Smart Search SDK"
requires-python = ">=3.11"
dependencies = [
    "gl-smart-search-sdk>=0.0.15",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
]
